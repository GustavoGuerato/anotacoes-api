// Definição do formato de um Usuário.
//
// TODO: crie e exporte uma interface Usuario com:
//   - id: number
//   - email: string
//   - senhaHash: string   <- NUNCA guarde a senha em texto puro, sempre o hash (bcrypt)
//
// DICA: quando for devolver o usuário numa resposta (ex: depois do login),
// nunca envie senhaHash pro cliente — monte um objeto novo só com id/email.
[];
